import React, {Fragment} from 'react'
import Navbar from '../../discrete/navbar'
import Login from '../../discrete/login'

const Header = () => (
    <Fragment>
        <Navbar/>
        <Login/>
    </Fragment>
)

export default Header